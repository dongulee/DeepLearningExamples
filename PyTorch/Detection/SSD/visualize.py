def visualize(img, bboxes, mode='save'):
    '''
    visualize an image and its bounding boxes
    img: PIL Image
    bboxes: list of bbox((l, t, r, b), lbl_text)
    '''
    
    # img save
    # jupyter notebook
    # separate window